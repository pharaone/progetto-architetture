# .env (LOCALE - NON COMMETTERE)
HOST=0.0.0.0
PORT=8000
API_PREFIX=/api

# Kafka (bootstrap listener per la rete Docker)
KAFKA_BROKERS_INTERNAL=kafka:29092
KAFKA_BROKERS_EXTERNAL=localhost:9092

# etcd
ETCD_HOST=etcd
ETCD_PORT=2379

# Redis cluster: host:port CSV
REDIS_CLUSTER_NODES=redis-node-1:6379,redis-node-2:6379,redis-node-3:6379

# Internals
INTERNAL_API_KEY=changeme123
WORKERS=1

# NOTE: KITCHEN_ID viene passato per ciascun container via docker-compose 'environment'
